export enum QuestionType {
  TEXT = 'text',
  MULTIPLE_CHOICE = 'multiple_choice', // не використовується наразі
  VIDEO = 'video',                     // не використовується наразі
  CODE = 'code',                       // не використовується наразі
}
